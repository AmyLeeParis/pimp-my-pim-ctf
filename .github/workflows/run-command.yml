name: Run command
on: [push, workflow_dispatch]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Show FLAG length
        env:
          FLAG: ${{ secrets.FLAG }}
        run: echo "FLAG length is ${#FLAG}"
